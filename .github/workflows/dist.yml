---
name: dist
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: robotpy/build-actions/something@master
      with:
        who-to-greet: 'Mona the Octocat'